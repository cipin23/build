name: Manual Build LineageOS 18.1

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Permissions & Run Script
        run: |
          chmod +x ulti.sh
          ./ulti.sh
